\akhead{Topic 2 — Linear Functions}

\ans{1}{1}{$19$}{Substitute $3$ for $x$: $f(3)=4(3)+7=12+7=19$.}
\ans{1}{2}{A}{The output at $x=0$ is the constant term: $f(0)=5(0)-8=-8$.}
\ans{1}{3}{B}{$f(4)=-2(4)+11=-8+11=3$. Keeping the sign of $-2$ is the whole
question.}
\ans{1}{4}{$5$}{Set $6a+5=35$, so $6a=30$ and $a=5$.}
\ans{1}{5}{C}{In $f(x)=mx+b$ the slope is $m$, the coefficient of $x$, so the
slope is $\frac{1}{2}$; $-4$ is the $y$-intercept.}
\ans{1}{6}{C}{The graph crosses the $y$-axis where $x=0$, and
$f(0)=-7(0)+12=12$, so the $y$-intercept is $(0,12)$.}
\ans{1}{7}{$3$}{Each increase of $1$ in $x$ raises $f(x)$ by $13-8=5$, so
going back one step from $x=1$ gives $f(0)=8-5=3$.}
\ans{1}{8}{C}{$h(6)=2(6)+15=12+15=27$ centimeters.}
\ans{1}{9}{B}{$15=h(0)$, the height when $t=0$, that is, at planting. The
$2$, not the $15$, is the weekly growth.}
\ans{1}{10}{$5$}{Slope
$=\dfrac{22-7}{5-2}=\dfrac{15}{3}=5$.}
\ans{1}{11}{B}{The slope is $\dfrac{21-9}{4-1}=4$, so stepping back one unit
from $x=1$ gives $f(0)=9-4=5$.}
\ans{1}{12}{A}{$f(0)=-4$ gives the constant term, and each step of $1$ in $x$
raises $f(x)$ by $3$, so $f(x)=3x-4$.}
\ans{1}{13}{A}{The hourly part is $285-60=225$ dollars, and
$225\div 15=15$ hours.}
\ans{1}{14}{A}{The coefficient of $t$ is the rate of change: the cost grows
by $\$6$ for each additional hour, so the rental costs $\$6$ per hour. The
$\$9$ is the fixed charge.}
\ans{1}{15}{C}{$f(x)$ rises $25-13=12$ while $x$ rises $5-2=3$, so the rate
of change is $\dfrac{12}{3}=4$ per unit increase in $x$.}
\ans{1}{16}{$4$}{$f(0)=-6$ and $f(4)=10$, so
$m=\dfrac{10-(-6)}{4-0}=\dfrac{16}{4}=4$.}
\ans{1}{17}{B}{Set $68-4h=40$, so $4h=28$ and $h=7$ hours.}
\ans{1}{18}{$48$}{From $f(6)=6a+8=32$, $a=4$; then
$f(10)=4(10)+8=48$.}
\ans{1}{19}{C}{The slope is $\dfrac{11-5}{3-1}=3$, and $x$ increases by
$7-1=6$ from the first column, so $k=5+3(6)=23$.}
\ans{1}{20}{C}{$t=12$ means $12$ years after $2000$, and the output is the
population, so in $2012$ the population was $6360$.}
\ans{1}{21}{$11$}{The slope is $\dfrac{15-3}{6-2}=3$, so
$f(x)=3(x-2)+3=3x-3$. Setting $3x-3=30$ gives $x=11$.}
\ans{1}{22}{A}{The slope is $\dfrac{6-9}{4-(-2)}=-\dfrac{1}{2}$, and
$f(4)=6$ gives $6=-\frac{1}{2}(4)+b$, so $b=8$ and
$f(x)=-\frac{1}{2}x+8$.}

\ans{2}{1}{A}{Set $5c+10=60$, so $5c=50$ and $c=10$.}
\ans{2}{2}{$1$}{The slope is $\dfrac{7-19}{4-0}=-3$, and $x$ increases by $2$
more, so $k=7+(-3)(2)=1$.}
\ans{2}{3}{B}{The input $3$ is hours driven and the output $60$ is the
distance still to be driven, so after $3$ hours $60$ miles remain.}
\ans{2}{4}{B}{The slope is $\dfrac{26-10}{7-3}=4$, so
$f(0)=10-4(3)=-2$.}
\ans{2}{5}{$18$}{The volume falls $320-194=126$ gallons in $12-5=7$ minutes,
so it falls $\dfrac{126}{7}=18$ gallons per minute.}
\ans{2}{6}{A}{The per-guest charge multiplies $n$ and the fixed fee is added
once: $C(n)=18n+75$.}
\ans{2}{7}{$2$}{$m=\dfrac{11-5}{4-2}=3$, and $5=3(2)+b$ gives $b=-1$, so
$m+b=3+(-1)=2$.}
\ans{2}{8}{A}{From $3k+6=27$, $3k=21$, so $k=7$.}
\ans{2}{9}{B}{The coefficient of $d$ is $-35$: each additional day lowers the
count by $35$ messages. The $250$ is the count at $d=0$.}
\ans{2}{10}{$2$}{The slope is $\dfrac{-7-11}{4-(-2)}=-3$, so
$f(1)=11+(-3)\bigl(1-(-2)\bigr)=11-9=2$.}
\ans{2}{11}{A}{From $y=4x-9$, add $9$ to both sides to get $y+9=4x$, then
divide by $4$: $x=\dfrac{y+9}{4}$.}
\ans{2}{12}{D}{The slope is $\dfrac{15-24}{3-0}=-3$ and $f(0)=24$, so
$f(x)=24-3x$; setting $24-3x=0$ gives $x=8$.}
\ans{2}{13}{C}{$f(3)=\dfrac{2}{3}(3)+5=2+5=7$, so $(3,7)$ is on the graph;
the other three inputs give $3$, $5$, and $9$.}
\ans{2}{14}{$9$}{$f(5)=15+b$ and $2f(1)=2(3+b)=6+2b$. Setting
$15+b=6+2b$ gives $b=9$.}
\ans{2}{15}{A}{A negative slope means the output falls as the input rises.
Since $b>0$, $g(0)=b$ is positive, and a line with $m<0$ eventually gives
negative outputs, so only the first statement must be true.}
\ans{2}{16}{B}{Between the first two columns $f(x)$ rises by $21$ while $x$
rises by $3$, so the slope is $\dfrac{21}{3}=7$; the constant $k$ cancels.}
\ans{2}{17}{$53$}{Each increase of $1$ in $x$ raises $f(x)$ by $6$, and $x$
goes from $2$ to $10$, so $f(10)=5+6(8)=53$.}
\ans{2}{18}{C}{Slope
$=\dfrac{19-4}{(t+3)-t}=\dfrac{15}{3}=5$; the constant $t$ subtracts away.}
\ans{2}{19}{A}{The input $50$ is gigabytes used in a month and the output
$36$ is the total monthly cost in dollars.}
\ans{2}{20}{$7$}{With slope $-2$ through $(3,8)$, $f(x)=8-2(x-3)$. Setting
$8-2(x-3)=0$ gives $x-3=4$, so $x=7$.}
\ans{2}{21}{B}{Between the two points $f(x)$ rises $21-9=12$, and slope $4$
means $c-2=\dfrac{12}{4}=3$, so $c=5$.}
\ans{2}{22}{B}{$f(4)=2(4)+5=13$, so $4k+11=13$, giving $4k=2$ and
$k=\dfrac{1}{2}$.}
